const slug=new URLSearchParams(location.search).get('slug');const article=ARTICLES.find(a=>a.slug===slug)||ARTICLES[0];document.title=`${article.title} · 陈真`;document.querySelector('#article-page').innerHTML=`<article><header class="article-hero"><a class="back" href="index.html#writing">← 返回文章列表</a><h1>${article.title}</h1><p class="dek">${article.dek}</p><div class="article-meta"><span>${article.date}</span><span>${article.category}</span><span>${article.reading}</span></div></header><div class="article-body">${article.sections.map(s=>`<h2>${s.title}</h2>${s.paragraphs.map(p=>p.startsWith('“')?`<blockquote>${p}</blockquote>`:`<p>${p}</p>`).join('')}`).join('')}<div class="article-end"><b>关于作者</b><p>陈真，AI 产品经理，持续研究 Agent、文档智能与人机协作。如果这篇文章帮你厘清了一个问题，欢迎分享给正在做 AI 产品的朋友。</p><a href="index.html#writing">继续阅读其他文章 →</a></div></div></article>`;
